<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.SizzelCraft" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <item name="colorPrimary">@color/yellow</item>
    </style>

    <style name="Theme.SizzelCraft" parent="Base.Theme.SizzelCraft" />

    <style name="ThemeOverlay.SizzelCraft.FullscreenContainer" parent="">
        <item name="fullscreenBackgroundColor">@color/fixed_white</item>
        <item name="fullscreenTextColor">@color/black</item>
    </style>
</resources>